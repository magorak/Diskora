<Window x:Class="Diskora.App.SmartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:Diskora.App.Converters"
        mc:Ignorable="d"
        Title="S.M.A.R.T." Height="560" Width="820" MinWidth="640" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Window.Resources>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <conv:RiskToBrushConverter x:Key="RiskToBrushConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding DiskName}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />

            <Border Grid.Column="1" Margin="0,0,12,0" Padding="10,4" CornerRadius="4"
                    Background="{Binding OverallHealth, Converter={StaticResource RiskToBrushConverter}}"
                    Visibility="{Binding IsSupported, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding OverallHealthDisplay}" Foreground="White" FontWeight="SemiBold" />
            </Border>

            <Button Grid.Column="2" Content="Obnovit" Command="{Binding RefreshCommand}" />
        </Grid>

        <Border Grid.Row="1"
                Background="{DynamicResource WarningBrush}"
                CornerRadius="4" Padding="10" Margin="0,0,0,12"
                Visibility="{Binding UnavailableReason, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding UnavailableReason}" Foreground="White" TextWrapping="Wrap" />
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <DataGrid ItemsSource="{Binding Attributes}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40" />
                    <DataGridTextColumn Header="Atribut" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Aktuální" Binding="{Binding CurrentValue}" Width="70" />
                    <DataGridTextColumn Header="Nejhorší" Binding="{Binding WorstValue}" Width="70" />
                    <DataGridTextColumn Header="Práh" Binding="{Binding Threshold}" Width="60" />
                    <DataGridTextColumn Header="Raw" Binding="{Binding RawValue}" Width="90" />
                    <DataGridTemplateColumn Header="Riziko" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Risk, Converter={StaticResource RiskToBrushConverter}}"
                                        CornerRadius="3" Padding="6,2" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding RiskDisplay}" Foreground="White" FontSize="11" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Margin="40,2,10,8" Style="{StaticResource MutedTextStyle}" />
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
                <DataGrid.RowDetailsVisibilityMode>VisibleWhenSelected</DataGrid.RowDetailsVisibilityMode>
            </DataGrid>
        </Border>
    </Grid>
</Window>
