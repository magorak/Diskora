<Window x:Class="Diskora.App.SystemEventLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:Diskora.App.Converters"
        mc:Ignorable="d"
        Title="Systémový protokol - disky a souborový systém" Height="640" Width="900" MinWidth="560" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Window.Resources>
        <conv:RiskToBrushConverter x:Key="RiskToBrushConverter" />
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Systémový protokol - disky a souborový systém" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Obnovit" Command="{Binding RefreshCommand}" />
        </Grid>

        <TextBlock Grid.Row="1" Margin="0,0,0,12" TextWrapping="Wrap" Style="{StaticResource MutedTextStyle}"
                   Text="Poslední události z protokolu událostí Windows týkající se disků a souborového systému (NTFS, stínové kopie, virtuální disky). Read-only - Diskora zde nic nezapisuje, jen zobrazuje, co už Windows sami zaznamenaly." />

        <TextBlock Grid.Row="2" Margin="0,0,0,12" Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True"
                          Visibility="{Binding HasEntries, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Kdy" Binding="{Binding TimeDisplay}" Width="150" />
                        <DataGridTemplateColumn Header="Úroveň" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Level, Converter={StaticResource RiskToBrushConverter}}"
                                            CornerRadius="3" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding LevelDisplay}" Foreground="White" FontSize="11" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Protokol" Binding="{Binding LogName}" Width="90" />
                        <DataGridTextColumn Header="Zdroj" Binding="{Binding ProviderName}" Width="200" />
                        <DataGridTextColumn Header="ID" Binding="{Binding EventId}" Width="50" />
                        <DataGridTextColumn Header="Popis" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="Žádné relevantní diskové/NTFS události v protokolu nejsou k dispozici."
                           Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding HasEntries, Converter={StaticResource InverseBooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
            </Grid>
        </Border>
    </Grid>
</Window>
