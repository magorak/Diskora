<Window x:Class="Diskora.App.SurfaceScanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:Diskora.App.Converters"
        mc:Ignorable="d"
        Title="Povrchový sken disku" Height="560" Width="640" MinWidth="480" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding DiskName}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Grid.Column="1" Margin="0,0,8,0" Content="Exportovat CSV..." Click="ExportCsv_Click" />
            <Button Grid.Column="2" Content="Exportovat JSON..." Click="ExportJson_Click" />
        </Grid>

        <TextBlock Grid.Row="1" Margin="0,0,0,12" TextWrapping="Wrap" Style="{StaticResource MutedTextStyle}"
                   Text="Sken je needestruktivní a pouze čte data disku - nic se nezapisuje ani neopravuje. Nečitelné oblasti obvykle znamenají vadné sektory; skutečná oprava je v Diskoře samostatný krok s vlastním potvrzením." />

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="0,0,8,0" Content="Spustit sken (jen čtení)"
                    Command="{Binding StartScanCommand}" Visibility="{Binding IsScanning, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <Button Grid.Column="1" Content="Zrušit" Command="{Binding CancelScanCommand}"
                    Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBlock Grid.Column="2" Text="{Binding Summary}" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap" />
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,12" Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding ProgressPercent, StringFormat={}Probíhá sken... {0:F0} %}" Margin="0,0,0,6" />
            <ProgressBar Grid.Row="1" Height="10" Minimum="0" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}"
                         AutomationProperties.Name="{Binding ProgressPercent, StringFormat='Průběh skenu {0:F0} procent'}" />
        </Grid>

        <Border Grid.Row="4" Style="{StaticResource CardBorderStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding BadRangeRows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
