<Window x:Class="Diskora.App.IntegrityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:Diskora.App.Converters"
        mc:Ignorable="d"
        Title="Kontrola integrity disku" Height="700" Width="760" MinWidth="560" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Window.Resources>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <conv:RiskToBrushConverter x:Key="RiskToBrushConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding VolumeName}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Grid.Column="1" Margin="0,0,8,0" Content="Exportovat CSV..." Click="ExportCsv_Click" />
            <Button Grid.Column="2" Margin="0,0,8,0" Content="Exportovat JSON..." Click="ExportJson_Click" />
            <Button Grid.Column="3" Content="Znovu zjistit stav" Command="{Binding RefreshDirtyStateCommand}" />
        </Grid>

        <Border Grid.Row="1" Margin="0,0,0,12" Padding="10,6" CornerRadius="4"
                Background="{Binding DirtyState, Converter={StaticResource RiskToBrushConverter}}">
            <TextBlock Text="{Binding DirtyStateDisplay}" Foreground="White" FontWeight="SemiBold" />
        </Border>

        <TextBlock Grid.Row="2" Margin="0,0,0,12" TextWrapping="Wrap" Style="{StaticResource MutedTextStyle}"
                   Text="Kontrola „/scan“ je needestruktivní a pouze čte data disku - svazek se neodpojuje a nic se neopravuje. Skutečná oprava nalezených chyb bude v Diskoře samostatný krok s vlastním potvrzením." />

        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="0,0,8,0" Content="Spustit kontrolu (jen čtení)"
                    Command="{Binding StartScanCommand}" Visibility="{Binding IsScanning, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <Button Grid.Column="1" Margin="0,0,8,0" Content="Zrušit" Command="{Binding CancelScanCommand}"
                    Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Grid.Column="2" Margin="0,0,8,0" Content="Opravit (spotfix)"
                    Command="{Binding RunSpotFixCommand}" Visibility="{Binding IsRepairing, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <Button Grid.Column="3" Margin="0,0,8,0" Content="Zrušit opravu" Command="{Binding CancelSpotFixCommand}"
                    Visibility="{Binding IsRepairing, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBlock Grid.Column="4" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap"
                       Text="{Binding ScanSummary}" Visibility="{Binding IsRepairing, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <TextBlock Grid.Column="4" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap"
                       Text="{Binding RepairSummary}" Visibility="{Binding IsRepairing, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,12"
              Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding CurrentStageDescription}" FontWeight="SemiBold" />
                <TextBlock Grid.Column="1" Text="{Binding ProgressPercent, StringFormat={}{0:F0} %}" Style="{StaticResource MutedTextStyle}" />
            </Grid>

            <ProgressBar Grid.Row="1" Height="10" Minimum="0" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}"
                         AutomationProperties.Name="{Binding ProgressPercent, StringFormat='Průběh kontroly {0:F0} procent'}" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,12"
              Visibility="{Binding IsRepairing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Probíhá oprava (spotfix)..." FontWeight="SemiBold" Margin="0,0,0,6" />
            <ProgressBar Grid.Row="1" Height="10" IsIndeterminate="True" AutomationProperties.Name="Probíhá oprava, průběh nelze určit" />
        </Grid>

        <Border Grid.Row="5" Style="{StaticResource CardBorderStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          ScrollChanged="OutputScroll_ScrollChanged"
                          AutomationProperties.Name="Výstup kontroly">
                <ItemsControl ItemsSource="{Binding OutputLines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="6" Text="Historie" Style="{StaticResource HeadingTextStyle}" Margin="0,16,0,8" />

        <Border Grid.Row="7" Style="{StaticResource CardBorderStyle}">
            <DataGrid ItemsSource="{Binding History}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kdy" Binding="{Binding TimestampDisplay}" Width="180" />
                    <DataGridTemplateColumn Header="Stav" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding DirtyState, Converter={StaticResource RiskToBrushConverter}}"
                                        CornerRadius="3" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DirtyStateDisplay}" Foreground="White" FontSize="11" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Sken" Binding="{Binding ScanDisplay}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
