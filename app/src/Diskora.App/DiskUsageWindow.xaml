<Window x:Class="Diskora.App.DiskUsageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:Diskora.App.Converters"
        mc:Ignorable="d"
        Title="Analýza zaplněnosti" Height="640" Width="900" MinWidth="620" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Window.Resources>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <conv:SeriesIndexToBrushConverter x:Key="SeriesIndexToBrushConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="↑ Nahoru" Margin="0,0,8,0"
                    Command="{Binding NavigateUpCommand}" />
            <TextBlock Grid.Column="1" Text="{Binding CurrentPathText}" FontSize="16" FontWeight="SemiBold"
                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
            <Button Grid.Column="2" Content="Exportovat CSV..." Margin="0,0,8,0" Click="ExportCsv_Click"
                    Visibility="{Binding IsScanning, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <Button Grid.Column="3" Content="Obnovit sken" Command="{Binding RescanCommand}" />
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding CurrentSummaryText}" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8" />

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12"
                Visibility="{Binding IsScanning, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel>
                <Border Height="22" CornerRadius="4" ClipToBounds="True" Background="{DynamicResource TrackBrush}">
                    <Grid x:Name="CompositionBarGrid" />
                </Border>
                <ItemsControl ItemsSource="{Binding CompositionSegments}" Margin="0,10,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,0,16,4" VerticalAlignment="Center"
                                        ToolTip="{Binding TooltipText}">
                                <Border Width="10" Height="10" CornerRadius="2" Margin="0,0,6,0"
                                        Background="{Binding SeriesIndex, Converter={StaticResource SeriesIndexToBrushConverter}}" />
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="{Binding Name, Mode=OneWay}" />
                                    <Run Text=" · " Foreground="{DynamicResource MutedForegroundBrush}" />
                                    <Run Text="{Binding PercentDisplay, Mode=OneWay}" Foreground="{DynamicResource MutedForegroundBrush}" />
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Grid.Row="3"
                Background="{DynamicResource DangerBrush}"
                CornerRadius="4" Padding="10" Margin="0,0,0,12"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="White" TextWrapping="Wrap" />
        </Border>

        <Grid Grid.Row="4">
            <TabControl x:Name="ResultsTabControl" Visibility="{Binding IsScanning, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <TabItem Header="Složky">
                    <Border Style="{StaticResource CardBorderStyle}">
                        <DataGrid ItemsSource="{Binding Items}" x:Name="ItemsGrid"
                                  MouseDoubleClick="ItemsGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="*" />
                                <DataGridTemplateColumn Header="Podíl" Width="160">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar Value="{Binding PercentOfParent, Mode=OneWay}"
                                                         Minimum="0" Maximum="100" VerticalAlignment="Center" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Velikost" Binding="{Binding SizeDisplay}" Width="110" />
                                <DataGridTextColumn Header="Souborů" Binding="{Binding FileCount}" Width="80" />
                                <DataGridTextColumn Header="Stav" Binding="{Binding StatusDisplay}" Width="120" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>

                <TabItem Header="Největší soubory">
                    <Border Style="{StaticResource CardBorderStyle}">
                        <DataGrid ItemsSource="{Binding LargestFiles}" IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="220" />
                                <DataGridTextColumn Header="Velikost" Binding="{Binding SizeDisplay}" Width="110" />
                                <DataGridTextColumn Header="Poslední změna" Binding="{Binding LastWriteDisplay}" Width="150" />
                                <DataGridTextColumn Header="Umístění" Binding="{Binding FullPath}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>

                <TabItem Header="Nejstarší soubory">
                    <Border Style="{StaticResource CardBorderStyle}">
                        <DataGrid ItemsSource="{Binding OldestFiles}" IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="220" />
                                <DataGridTextColumn Header="Poslední změna" Binding="{Binding LastWriteDisplay}" Width="150" />
                                <DataGridTextColumn Header="Velikost" Binding="{Binding SizeDisplay}" Width="110" />
                                <DataGridTextColumn Header="Umístění" Binding="{Binding FullPath}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>
            </TabControl>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Prohledávám disk..." HorizontalAlignment="Center" FontSize="14" Margin="0,0,0,8" />
                <TextBlock Text="{Binding ScanningStatusText}" HorizontalAlignment="Center"
                           Style="{StaticResource MutedTextStyle}" TextTrimming="CharacterEllipsis" MaxWidth="700" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
