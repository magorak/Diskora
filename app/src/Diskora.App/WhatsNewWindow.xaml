<Window x:Class="Diskora.App.WhatsNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:display="clr-namespace:Diskora.App.Display"
        mc:Ignorable="d"
        Title="Co je nového" Height="640" Width="760" MinWidth="520" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/AppIcon.ico">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Heading}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          AutomationProperties.Name="Seznam změn podle verzí">
                <ItemsControl ItemsSource="{Binding Releases}" Margin="12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="{Binding Title}" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,8" />

                                <ItemsControl ItemsSource="{Binding Sections}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,0,0,12">
                                                <TextBlock Text="{Binding Category}" FontWeight="SemiBold" Margin="0,0,0,4" />

                                                <ItemsControl ItemsSource="{Binding Items}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="8,0,0,6">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="•" Margin="0,0,8,0" VerticalAlignment="Top" />
                                                                <TextBlock Grid.Column="1" TextWrapping="Wrap"
                                                                           display:ChangelogInlines.Segments="{Binding Segments}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource MutedTextStyle}"
                       Text="Stejný přehled je i v online dokumentaci." TextWrapping="Wrap" />

            <Button Grid.Column="1" Margin="0,0,8,0" Content="Otevřít na webu..." Click="OpenOnWeb_Click" />
            <Button Grid.Column="2" Content="Zavřít" IsDefault="True" IsCancel="True" Click="Close_Click" />
        </Grid>
    </Grid>
</Window>
